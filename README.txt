Admin Name & Password are set to
Uname: 'Drew'
Password: 'test'

when viewing indexdb it shows password as 'lukl'
as password uses a cypher for each char of:

(n+15)mod24

so 'test' is stored as lukl :)